@namespace HITSBlazor.Pages.Admin.AllTags
@using HITSBlazor.Components.Button
@using HITSBlazor.Components.Filters.RadioFilter
@using HITSBlazor.Components.Filters.ResetFilterButton
@using HITSBlazor.Components.InputSearch
@using HITSBlazor.Components.ListPageComponents.ListPageHeader
@using HITSBlazor.Components.ListPageComponents.ListPageSearch
@using HITSBlazor.Components.ListPageComponents.ListPageTable
@using HITSBlazor.Components.Placeholders.TablePlaceholder
@using HITSBlazor.Components.Statuses.BoolStatus
@using HITSBlazor.Components.Tables.TableActionMenu
@using HITSBlazor.Components.Tables.TableBodyRow
@using HITSBlazor.Components.Tables.TableComponent
@using HITSBlazor.Components.Tables.TableHeader
@using HITSBlazor.Utils.Models
@inherits ListPageDataLoader

<div class="page-content-with-header bg-white overflow-y-scroll">
    <div class="w-100 bg-white p-3">
        <ListPageHeader IsLoading="_isLoading"
                        HeaderText="@($"Список тегов: {_totalCount}")"
                        CreateButtonText="Добавить компетенцию"
                        ShowCreateButton="true"
                        OnButtonClick="() => ShowTagModal()" />
        <ListPageSearch IsLoading="_isLoading" ValueChanged="@((value) => SeacrhTag(value))" />
        <InputSearch Width="50" IsLoading="_isLoading" ValueChanged="@((value) => SeacrhTag(value))" DebounceDelay="500" />
        <TableComponent IsLoading="_isLoading" TableActionMenuIsEnable="true" HeaderItems="_tagsTableHeader" @ref="_tableComponent">
            <TableContent>
                @foreach (var tag in _tags)
                {
                    <tr>
                        <TableBodyRow IsCentered="true">
                            <div class="bi bi-circle-fill fs-6 mt-1 px-2 py-1" style="@($"color: {tag.Color};")">
                                <div></div>
                            </div>
                        </TableBodyRow>
                        <TableBodyRow>
                            <div class="data-link px-2 py-1" @onclick="() => ShowTagModal(tag)">
                                @tag.Name
                            </div>
                        </TableBodyRow>
                        <TableBodyRow>
                            <BoolStatus StatusType="BoolStatusType.TagConfirm" Value="tag.Confirmed" />
                        </TableBodyRow>
                        <TableActionMenu ItemId="@tag.Id"
                                         OnAction="OnTagAction"
                                         ActionIds="@GetTableActions(tag)" />
                    </tr>
                }
            </TableContent>
            <FilterContent>
                <RadioFilter T="ValueViewModel<bool?>"
                             IsLoading="_isLoading"
                             Label="Статус"
                             AllValues="_confirmedFilterValues"
                             @bind-Value="IsConfirmed"
                             OnFilterChanged="FiltersHasChanged" />
                <ResetFilterButton IsLoading="_isLoading" ResetMethod="ResetFilters" />
            </FilterContent>
        </TableComponent>
    </div>
</div>